import { tw } from "../lib/tw";

export const paginationRoot = tw`mt-4 flex flex-wrap items-center justify-between gap-3`;
export const paginationStatus = tw`text-muted text-sm`;
export const paginationControls = tw`flex flex-wrap items-center gap-2`;
export const paginationSelect =
  tw`border-border text-ink shadow-small rounded-xl border bg-white px-4 py-2 text-sm`;
export const paginationLabel = tw`text-sm`;
